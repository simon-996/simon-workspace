# Simon Workspace Web

Vue 3 frontend for Simon Workspace.

## Local Commands

```bash
npm install
npm run dev
npm run build
```

The development server proxies `/api` requests to `http://localhost:8080`.
